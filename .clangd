# .clangd configuration file
# Specifies the location of the compilation database for clangd.

CompileFlags:
  # Uses compile_commands.json from the 'build' directory.
  CompilationDatabase: build

Diagnostics:
  # Suppress diagnostics from system and standard library files
  Suppress:
    # Common warnings to suppress for fast development
    - unused-includes
